# Coding Conventions & Best Practices

## General C# Conventions

Keep standard .NET conventions
Keep configuration for database and other infrastracture modules in explicit way
Do not add attributes when it can be configured explicit (like BsonClass for example)
